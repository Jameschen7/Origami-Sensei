from .dog_config import dog_config
from .whale_config import whale_config
from .cat_config import cat_config

ori_model_name2config = {
    "Dog": dog_config,
    "Whale": whale_config,
    "Cat": cat_config,
}